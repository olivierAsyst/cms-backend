spring:
  application:
    name: tumainiafricanews

  datasource:
    url: jdbc:postgresql://localhost:5432/tumainidb
    username: postgres
    password: Asyst@123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

jwt:
  secret: 4B6150645367566B5970337336763979244226452948404D635166546A576E5A
  expiration: 86400000

#app:
#  cors:
#    # Origines autorisées (séparées par des virgules)
#    allowed-origins: http://localhost:4200,http://localhost:3000,https://yourdomain.com

# Server
server:
  port: 8081
  servlet:
    context-path: /api/tumainiafricanews
  error:
    include-message: always
    include-binding-errors: always
    include-stacktrace: on_param
    include-exception: false
  compression:
    enabled: true
    mime-types: application/json,application/xml,text/html,text/xml,text/plain

#minio:
#  endpoint: http://localhost:9000
#  access-key: minioadmin
#  secret-key: minioadmin123
#  bucket: tumaini-news

minio:
  server:
    url: http://localhost:7000
    access-key: minioadmin
    secret-key: minioadmin123
  bucket-name: tumaini-news

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html